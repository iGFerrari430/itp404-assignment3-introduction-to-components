import React from 'react';

export default class Loader extends React.Component {
    render() {
        return (
            <div className="load-container load2"><div className="loader">Loading...</div></div>
        )
    }
}